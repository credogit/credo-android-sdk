<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.impact.credopayment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Payment">
        <activity android:name=".activities.ThirdPartyPay"/>
        <activity android:name=".activities.SuccessActivity"/>
        <activity android:name=".activities.VerifyAccount"/>
    </application>

</manifest>